import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    h1: {
      fontSize: "2.5rem"
    },
    h2: {
      fontSize: "2rem"
    }
  }
});

export default theme;
